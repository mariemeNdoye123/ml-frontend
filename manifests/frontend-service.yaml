apiVersion: v1
kind: Service
metadata:
  name: frontend-service
  namespace: devops-project
spec:
  type: NodePort
  ports:
    - protocol: TCP
      port: 3000
      targetPort: 3000
      nodePort: 30001
  selector:
    app: frontend
